# Agent Work Records

This directory holds per-task work records for agents working on the
MedReader Agent project at `/home/z/my-project`.

> Note: the canonical path requested by the orchestrator is `/agent-ctx`,
> but that path is not writable in this sandbox (`Permission denied`).
> Work records are stored here as a fallback so other agents can still
> read them.
